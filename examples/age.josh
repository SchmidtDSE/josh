start unit year

  alias years
  alias yr
  alias yrs

end unit

start organism ForeverTree

  age.init = 1 year

  age.step = {
    const currentAge = prior.age
    const newAge = currentAge + 1 year
    return newAge
  }

end organism
